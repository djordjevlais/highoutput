# Site settings
title: High Output Ventures
version: "v1"
description: "Startup studio that builds and invests."
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://www.highoutput.io" # the base hostname & protocol for your site

# Tachyons Version
tv: "4.7"

# Build settings
markdown: kramdown

# Exclude this stuff
exclude: [node_modules, gulpfile.js]

# Gems
gems:
  - jekyll-seo-tag
